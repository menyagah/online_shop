import {Router} from 'express';


const router = Router();


/**
 * Product routes
 */
router.get('/product', ()=>{})
router.get('/product/:id', ()=>{})
router.post('/product', ()=>{})
router.delete('/product/:id', ()=>{})

export default router;